# v0.5.7 거래 안전/UI
- 내가 주는 자원/화폐는 현재 보유량을 최대값으로 제한
- 보유량 0 항목은 회색 + 숫자 입력/증감 모두 비활성
- +/- 버튼 및 직접 입력 모두 max를 넘을 수 없음
- 받고 싶은 자원은 상대 재고를 조회하지 않고 자유 제안
- 직접 계약 생성 시 서버는 발신자 offer만 검증
- 게시판 등록 시 서버는 게시자의 offer만 검증
- 최종 수락 시 서버는 양측 재고를 비공개 재검증
- 실패 시 상대방의 실제 재고 수량/부족 품목은 공개하지 않음
- 받은 거래 요청은 화면 중앙 대형 모달로 표시
- 본인이 줄 자원이 부족하면 거래하기 버튼 비활성
