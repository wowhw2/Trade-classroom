# v0.5.5 학생 미리보기 구조 수정

이전 접근의 핵심 문제가 확인되었습니다.

교사 화면의 `#app`은 서버 state를 받을 때마다 `innerHTML` 전체가 교체됩니다.
iframe을 잠깐 DOM에서 분리했다 다시 붙이는 방식도 브라우저가 iframe의 browsing context를
재생성할 수 있어 반복 로드/깜빡임/자동 참가 재시도를 일으켰습니다.

v0.5.5에서는 구조를 바꿨습니다.

- `teacher.html`에 `#previewHost`를 `#app` 바깥의 독립 영역으로 둡니다.
- 교사 화면이 몇 번 다시 렌더링되어도 `#previewHost`와 iframe은 건드리지 않습니다.
- 같은 모둠을 보는 동안 iframe URL/DOM을 절대 재생성하지 않습니다.
- 다른 모둠을 선택할 때만 iframe을 한 번 새로 만듭니다.
- 따라서 자동 참가도 한 번만 수행되고 학생 화면 상태가 유지됩니다.
