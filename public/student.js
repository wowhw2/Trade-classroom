const s=io(),q=new URLSearchParams(location.search),room=q.get('room'),app=document.querySelector('#app');let me=localStorage.getItem('trade_'+room),st;
const I={food:'🍚',wood:'🪵',car:'🚗',oil:'🛢️',textile:'🧵',tech:'💡'},K=Object.keys(I),NM={food:'식량',wood:'목재',car:'자동차',oil:'석유',textile:'섬유',tech:'기술력'};
const ND={white:['⚪ 하얀 나라','food',{food:2,wood:3,car:1,oil:1,textile:3,tech:2}],brown:['🟤 갈색 나라','wood',{food:2,wood:2,car:2,oil:2,textile:2,tech:2}],red:['🔴 빨간 나라','car',{food:2,wood:1,car:3,oil:2,textile:3,tech:1}],black:['⚫ 검은 나라','oil',{food:2,wood:1,car:3,oil:2,textile:1,tech:3}],blue:['🔵 파란 나라','textile',{food:2,wood:2,car:1,oil:3,textile:2,tech:2}],yellow:['🟡 노란 나라','tech',{food:2,wood:3,car:2,oil:2,textile:1,tech:2}]};
function join(n=''){s.emit('join',{code:room,name:n,teamId:me})}if(room&&me)join();document.querySelector('#join')?.addEventListener('click',()=>join(document.querySelector('#name').value));
s.on('joined',id=>{me=id;localStorage.setItem('trade_'+room,id)});s.on('msg',alert);s.on('joinDenied',d=>{app.innerHTML=`<section class=card><h1>🔒 참가가 마감되었습니다</h1><p>${d.reason}</p></section>`});s.on('teamRemoved',d=>{if(d.teamId!==me)return;localStorage.removeItem('trade_'+room);me=null;app.innerHTML=`<section class=card><h1>수업방에서 나왔어요</h1><p>잘못 접속했다면 QR을 다시 찍고 올바른 모둠 이름으로 참가하세요.</p><button id=rejoin>다시 참가하기</button></section>`;document.querySelector('#rejoin').onclick=()=>location.reload()});s.on('state',x=>{st=x;render()});
function pack(p){return [...K.filter(k=>p?.[k]).map(k=>I[k]+NM[k]+' '+p[k]),p?.cash?'💰 '+p.cash:''].filter(Boolean).join(' + ')||'없음'}
function inputs(pre){return `<div class=resource>${K.map(k=>`<label>${I[k]} ${NM[k]}<input type=number min=0 max=99 value=0 data-${pre}="${k}"></label>`).join('')}<label>💰 화폐<input type=number min=0 max=999 value=0 data-${pre}="cash"></label></div>`}
function values(pre){let o={};document.querySelectorAll(`[data-${pre}]`).forEach(e=>o[e.getAttribute(`data-${pre}`)]=Math.max(0,+e.value||0));return o}
function render(){let t=st?.teams.find(x=>x.id===me);if(!t)return;let n=t.nation&&ND[t.nation];app.innerHTML=`<section class=card><div class=muted>${t.name}</div><h1>${n?n[0]:'나라 배정 대기 중'}</h1><div class=big>💰 ${t.cash}</div></section>`;if(!n){app.innerHTML+=`<section class=card>선생님이 나라를 배정하고 있어요.</section>`;return}app.innerHTML+=`<section class=card><b>ROUND ${st.round}</b><p>주력 생산품 ${I[n[1]]} ${NM[n[1]]}</p><div>${K.map(k=>`<span class=pill>${I[k]} ${NM[k]} ${t.inv[k]} ${t.inv[k]<n[2][k]?'<b class=warn>부족</b>':''}</span>`).join('')}</div></section>`;if(st.phase==='intro')app.innerHTML+=`<section class=card><h2>게임 방법</h2><p>🏭 생산 → 🤝 무역 → 📦 소비 순서로 진행해요.</p><p>친구와 직접 이야기하고 합의하면 iPad에서 계약하세요.</p><p>남는 자원은 종류별 최대 <b>${st.settings.stockCap}개</b>만 보관할 수 있어요.</p></section>`;else if(st.phase==='buildIntro')app.innerHTML+=`<section class=card><h2>🏗️ 건설이 열렸어요!</h2><p>시설은 생산량이나 소비 보상을 높여 줍니다.</p><p>시설에 쓴 돈도 최종 <b>시설 자산</b>으로 계산됩니다.</p></section>`;else if(st.phase==='results'&&t.last)app.innerHTML+=`<section class=card><h2>📦 소비·재고 정리</h2><p>부족 <b>${t.last.miss}</b>개 · 보상 <b class=good>+${t.last.reward}</b> · 폐기 <b class=warn>${t.last.waste}</b>개</p></section>`;else if(st.phase==='trade')trade(t);else if(st.phase==='build')build(t);else if(st.phase==='final')final(t);else app.innerHTML+=`<section class=card><h2>잠시 기다려 주세요</h2><p>선생님이 다음 단계를 시작합니다.</p></section>`}
function trade(t){let others=st.teams.filter(x=>x.id!==t.id&&x.nation);app.innerHTML+=`<section class=card><h2>🤝 직접 거래 계약</h2><select id=to>${others.map(x=>`<option value=${x.id}>${ND[x.nation][0]} · ${x.name}</option>`).join('')}</select><h3>우리가 줄 것</h3>${inputs('give')}<h3>우리가 받을 것</h3>${inputs('want')}<button id=send>계약서 보내기</button></section><section class=card><h2>📢 거래 게시판</h2>${st.posts.filter(p=>p.active&&p.teamId!==t.id).map(p=>{let a=st.teams.find(x=>x.id===p.teamId);return `<p><b>${ND[a.nation][0]}</b> ${pack(p.offer)} ↔ ${pack(p.want)} <button class=take data-id=${p.id}>거래 신청</button></p>`}).join('')||'아직 게시물이 없어요.'}<details><summary>내 거래 올리기</summary><h3>줄 것</h3>${inputs('pg')}<h3>받고 싶은 것</h3>${inputs('pw')}<button id=post>게시하기</button></details></section>`;let incoming=st.contracts.filter(c=>c.to===t.id&&c.status==='pending');if(incoming.length)app.innerHTML+=`<section class=card><h2>📨 계약서가 왔어요</h2>${incoming.map(c=>{let a=st.teams.find(x=>x.id===c.from);return `<p><b>${ND[a.nation][0]}</b><br>받기: ${pack(c.offer)}<br>주기: ${pack(c.want)}<br><button class=yes data-id=${c.id}>✅ 계약</button><button class="no alt" data-id=${c.id}>❌ 거절</button></p>`}).join('')}</section>`;document.querySelector('#send').onclick=()=>s.emit('contract',{to:document.querySelector('#to').value,offer:values('give'),want:values('want')});document.querySelector('#post').onclick=()=>s.emit('post',{offer:values('pg'),want:values('pw')});document.querySelectorAll('.take').forEach(b=>b.onclick=()=>s.emit('take',{id:b.dataset.id}));document.querySelectorAll('.yes').forEach(b=>b.onclick=()=>s.emit('answer',{id:b.dataset.id,ok:true}));document.querySelectorAll('.no').forEach(b=>b.onclick=()=>s.emit('answer',{id:b.dataset.id,ok:false}))}
function build(t){app.innerHTML+=`<section class=card><h2>🏗️ 시설 건설</h2><div class=grid>${st.facilities.map(f=>`<div class=card><b>${f.name}</b><p>💰 ${f.cost}</p><p>${f.type==='production'?'🏭 생산량':'🎁 소비 보상'} +${f.value}</p><button class=bld data-id=${f.id} ${t.cash<f.cost||t.buildings.some(x=>x.id===f.id)?'disabled':''}>건설</button></div>`).join('')}</div></section>`;document.querySelectorAll('.bld').forEach(b=>b.onclick=()=>s.emit('build',b.dataset.id))}
function final(t){let fv=t.buildings.reduce((a,b)=>a+b.cost,0);app.innerHTML+=`<section class=card><h2>🏁 우리나라 최종 결과</h2><p>💰 남은 자본 ${t.cash}</p><p>🏗️ 시설 자산 ${fv}</p><p class=big>총자산 ${t.cash+fv}</p><p>🤝 거래 ${t.stats.trades}회 · ✅ 완전충족 ${t.stats.full}회 · ♻️ 폐기 ${t.stats.waste}개</p></section>`}
