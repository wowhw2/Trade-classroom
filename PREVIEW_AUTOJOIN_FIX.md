# v0.5.4 학생 미리보기 자동 참가 수정

원인:
Socket.IO 객체 생성 직후 연결이 매우 빠르게 완료되면, 이전 버전의 `connect` 이벤트 리스너가
등록되기 전에 connect 이벤트가 지나갈 수 있었습니다. 이 경우 previewTeam ID는 있어도
join 이벤트를 서버에 보내지 않아 초기 '모둠 이름 입력' 화면에 남았습니다.

수정:
- 모든 Socket.IO 이벤트 리스너를 먼저 등록합니다.
- 이미 연결된 경우 `queueMicrotask()`로 즉시 기존 모둠에 참가합니다.
- 아직 연결 전이면 `once('connect')`로 참가합니다.
- preview 모드에서는 초기 모둠 이름 입력 화면을 보여주지 않고
  '학생 화면 불러오는 중…' 화면을 먼저 표시합니다.
- 서버 state를 받으면 선택한 테스트 모둠/나라 화면으로 전환됩니다.
